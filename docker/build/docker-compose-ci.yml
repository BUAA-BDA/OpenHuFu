version: '3.3'

services:
  builder:
    image: maven:3.8-openjdk-11
    container_name: builder
    restart: "no"
    working_dir: ${MOUNT_POINT}
    command: bash package.sh
    volumes:
      - $HOME/.m2:/root/.m2
      - ${MOUNT_POINT}:/usr/src/build