<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hufudb.onedb.backend.mapper.SqlRecordMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.hufudb.onedb.backend.entity.SqlRecord">
        <id column="id" property="id" />
        <result column="context" property="context" />
        <result column="user_name" property="userName" />
        <result column="status" property="status" />
        <result column="start_time" property="startTime" />
        <result column="end_time" property="endTime" />
    </resultMap>

    <select id="selectRecord" resultType="com.hufudb.onedb.backend.entity.SqlRecord">
        SELECT * FROM sql_record WHERE context RLIKE #{context} AND status RLIKE #{status}
    </select>

    <insert id="insertRecord" parameterType="com.hufudb.onedb.backend.entity.SqlRecord">
        INSERT INTO sql_record(context, user_name, status) VALUES (#{context}, #{userName}, "In Progress")
        <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>

    <update id="updateStatus">
        UPDATE sql_record SET status = #{status} where id = #{id}
    </update>
</mapper>
